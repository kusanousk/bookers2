<%= form_with model: [@book, @book_comment], local: false do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, class: 'form-control', id: 'comment-input' %>
  </div>
  <div class="form-group">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>
